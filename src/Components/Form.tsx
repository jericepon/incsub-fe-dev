import React from 'react'
import Button from './Button'

const Form = () => {
    return (
        <form>
            <Button disabled={false} variant="block" text='Next' />
        </form>
    )
}

export default Form
